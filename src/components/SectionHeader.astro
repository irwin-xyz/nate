---
import { formatDate, generatedAt, slug } from "../lib/site.js";

const { title, body, source } = Astro.props;
---
<header class="section-header">
  <h1 id={slug(title)}>{title}</h1>
  <p>{body}</p>
  <p class="source-note">Generated {formatDate(generatedAt)} from {source}.</p>
</header>
